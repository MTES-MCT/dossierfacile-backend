<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <!-- The partner integrated the COMPLETED status: it sees it in its payloads, receives the
         COMPLETED_ACCOUNT webhook and no longer sends a COMPLETED dossier back to the queue on
         link. Only effective while the partner_completed_optin flag below is active. -->
    <changeSet id="20260910000000-1" author="dossierfacile">
        <addColumn tableName="user_api">
            <column name="completed_status_supported" type="BOOLEAN" defaultValueBoolean="false">
                <constraints nullable="false"/>
            </column>
        </addColumn>
        <rollback>
            <dropColumn tableName="user_api" columnName="completed_status_supported"/>
        </rollback>
    </changeSet>

    <!-- Global kill-switch (rollout_pct and only_for_new_user are ignored) for the partners
         flagged user_api.completed_status_supported. Inactive: no partner sees COMPLETED. -->
    <changeSet id="20260910000000-2" author="dossierfacile">
        <insert tableName="feature_flag">
            <column name="key" value="partner_completed_optin"/>
            <column name="description" value="Global kill-switch: when active, the partners flagged completed_status_supported (user_api) see the COMPLETED status, receive the COMPLETED_ACCOUNT webhook and no longer send a COMPLETED dossier back to the queue on link"/>
            <column name="active" valueBoolean="false"/>
            <column name="only_for_new_user" valueBoolean="false"/>
            <column name="rollout_pct" valueNumeric="100"/>
        </insert>
        <rollback>
            <delete tableName="feature_flag">
                <where>key = 'partner_completed_optin'</where>
            </delete>
        </rollback>
    </changeSet>

</databaseChangeLog>
